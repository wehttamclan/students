<!DOCTYPE html>
<html>
  <head>
    <title>Students</title>
  </head>
  <body>
    <h2>All Students</h2>
    <div class="students">
      <ul id="#{student.name}">
        <% @students.each do |student| %>
        <li> <%= link_to "Delete #{student.name}", student_path(student), method: 'delete' %> <%= link_to student.name, edit_student_path(student) %></li>
        <% end %>
      </ul>
    </div>
  </body>
</html>

<!DOCTYPE html>
<html>
  <head>
    <title>Students</title>
  </head>
  <body>
    <h2>All Students</h2>
    <div class="students">
      <ul id="#{student.name}">
        <% @students.each do |student| %>
        <li> <%= link_to "Delete", students_path, method: 'delete' %> <%= link_to student.name, edit_student_path(student) %></li>
        <% end %>
      </ul>
    </div>
  </body>
</html>
